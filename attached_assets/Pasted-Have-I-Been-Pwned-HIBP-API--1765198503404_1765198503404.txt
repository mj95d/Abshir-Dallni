Have I Been Pwned (HIBP API)

وهو:
✔ قانوني
✔ معتمد عالميًا
✔ يعطيك التسريبات الحقيقية
✔ يحمي خصوصية المستخدم (K-Anonymity)

🔥 كيف أستخدم HIBP في موقعك؟

عندك خيارين:

الخيار 1️⃣ API Official (مدفوع)

إذا تبي نتائج كاملة لأي بريد:
https://haveibeenpwned.com/API/v3

لكن يحتاج:

API Key

دفع (لكن رخيص جدًا)

الخيار 2️⃣ Password Leak Checker — مجاني

طريقة K-Anonymity:
تُرسل أول 5 حروف من الهاش فقط → ما ترسل الباسورد نفسه.
ممتاز للأمان.

✨ الأفضل لك لموقع "دلّني"

هو الخيار 1 (Email Breach Check) لأنك تفحص بريد إلكتروني وليس كلمة مرور.

🎉 الآن أعطيك الكود الجاهز للربط
📌 ملف API backend — تعملها في المسار:

/api/checkBreach.js

Node.js / Vercel Serverless Function
import fetch from "node-fetch";

export default async function handler(req, res) {
  const { email } = req.query;

  if (!email) {
    return res.status(400).json({ error: "Email is required" });
  }

  const apiKey = process.env.HIBP_API_KEY;

  try {
    const response = await fetch(
      `https://haveibeenpwned.com/api/v3/breachedaccount/${email}`,
      {
        headers: {
          "hibp-api-key": apiKey,
          "user-agent": "DallniCyberCheck"
        }
      }
    );

    if (response.status === 404) {
      return res.status(200).json({ breached: false, breaches: [] });
    }

    const data = await response.json();
    return res.status(200).json({ breached: true, breaches: data });

  } catch (error) {
    return res.status(500).json({ error: "Internal Error", details: error });
  }
}

📌 ثاني خطوة: ربط واجهة الصفحة بزر (Front-end)
Inside your HTML/JS page:
async function checkBreach() {
  const email = document.getElementById("emailInput").value;

  const res = await fetch(`/api/checkBreach?email=${email}`);
  const data = await res.json();

  const resultBox = document.getElementById("result");

  if (data.breached === false) {
    resultBox.innerHTML = "✔️ بريدك آمن — لم يتم العثور على أي تسريب.";
  } else {
    resultBox.innerHTML = `
      ⚠️ بريدك مسرّب!<br>
      عدد التسريبات: ${data.breaches.length}<br>
      <ul>
        ${data.breaches.map(b => `<li>${b.Name} (${b.Domain})</li>`).join("")}
      </ul>
    `;
  }
}

📌 ثالث خطوة: اربطها بزر “فحص الآن”

في HTML:

<input id="emailInput" type="email" placeholder="أدخل بريدك الإلكتروني" />
<button onclick="checkBreach()">فحص الآن</button>

<div id="result"></div>